<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Example1" Height="300" Width="452.239">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition />
      <RowDefinition />
      <RowDefinition />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="{Binding text1}" />
    <TextBlock Grid.Row="1" Text="{Binding text2}" />
    <Button Grid.Row="2"  Content="Send signal" Command="{Binding send_signal}" />
  </Grid>
</Window>